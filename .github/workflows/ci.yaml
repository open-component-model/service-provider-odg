name: CI

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - v*

permissions:
  contents: read

jobs:
  build_validate_test:
    uses: openmcp-project/build/.github/workflows/ci.lib.yaml@dcab75e52b34789ede669947300504d208d06ae3 # main
    secrets: inherit
