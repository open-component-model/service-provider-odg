name: Renovate Generate

on:
  push:
    branches:
      - "renovate/**"

permissions:
  contents: write

jobs:
  generate:
    uses: openmcp-project/build/.github/workflows/renovate-generate.lib.yaml@25cc8d1c795127087fa7f26b278b23ae8b35478a # main
    secrets: inherit
    with:
      app_id: ${{ vars.ODG_BOT_APP_ID }}
      github-environment: automation
